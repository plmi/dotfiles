require("catppuccin").setup({
  flavour = "macchiato",
  integrations = {
    telescope = { enabled = true },
    treesitter = true,
    markdown = true,
    treesitter = true,
    which_key = false
  }
})
