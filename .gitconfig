[user]
	name = plmi
	email = 65455617+plmi@users.noreply.github.com
[alias]
  st = status
  co = checkout
  uncommit = reset HEAD^
  tree = log --all --graph
  unstage = reset HEAD -- .
  cl = reset --hard
  dellocal = branch -d
  delremote = push origin --delete
  fap = fetch --all --prune
[core]
	editor = nvim
